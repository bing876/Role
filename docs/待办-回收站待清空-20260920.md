# 待办：清空回收站（**不要立即执行**）

> 建档：2026-09-20
> 状态：⏳ **等待观察期结束**
> 关联：`docs/可删除文件清单-20260920.md`、`_deleted_manifest.txt`

---

## 为什么有这个待办

2026-09-20 的保守清理**走的是回收站**（可恢复），所以那 **≈880 MB 并没有真正释放** ——
文件还在盘上。要等**清空回收站**才真正腾出空间。

**当前磁盘可用空间与清理前完全一样**，这是预期现象，不是没删掉。

---

## 执行前置条件（**全部满足才能清空**）

按你的要求：**先观察 1~2 天**，确认没有任何"少了什么东西"的异常。逐条打勾：

- [ ] **观察期 ≥ 2 天**（建议 2026-09-22 之后再考虑）
- [ ] 应用能正常启动：双击 `start-dev.cmd` → 登录进工作台
- [ ] 构建正常：`npm run build -w @ai-workbench/shared|server`、`npm run build -w @ai-workbench/desktop`、`build:electron`
- [ ] 验收脚本正常：`pause-resume-tests.py` 仍能跑通（它依赖 `scripts/verify/` 下的脚本与假页面）
- [ ] **换装流程正常**：`swap-dist.mjs` 仍能把旧 asar 归档进 `_rollback-backup-20260918/`
      （本次保留了该目录与 `app.asar.old-*`，删掉的话这一步会静默跳过归档）
- [ ] 没有出现"某个测试/脚本找不到文件"的报错
- [ ] 确认 **`apps/desktop/release-frameless` / `release-ui15` 本体确实不需要了**
      （本次保留了它们；如果你后来发现需要旧安装包，说明这两份留对了）

---

## 清空前的最后一道保险

清空**不可逆**。执行前请确认：

1. **确认真的不需要恢复**其中任何一项 —— 若要留保险，
   可先把 `_deleted_manifest.txt` 里列的关键路径**从回收站还原**出来再清空；
2. 清空后回收站里的 482 条记录会消失，**`_deleted_manifest.txt` 仍会保留**（它是文字清单，不在回收站里）。

---

## 清空方式（等你确认后由我执行）

- 方式一：资源管理器 → 回收站 → 清空回收站
- 方式二（更精确）：只删本次那 482 条 `$I/$R`，不碰历史项

**建议用方式二** —— 这个回收站里有 8800+ 条历史记录，整体清空会连带删掉你以前误删想找回的东西。
本次的 482 条可以按 `$I` 元数据里的原始路径精确识别（关键词：
`work123\_rollback-backup-20260918\release-*`、`work123\_e2e-profile`、
`work123\_probe-profile-fresh`、`work123\scripts\verify\__pycache__`、`NVIDIA Corporation`）。

---

## 本次进回收站的内容（备忘）

| 内容 | 记录数 | 大小 |
| --- | --- | --- |
| `_rollback-backup-20260918/release-frameless` | 86 | 427.2 MB |
| `_rollback-backup-20260918/release-ui15` | 85 | 427.8 MB |
| `_e2e-profile` | 219 | 15.7 MB |
| `_probe-profile-fresh` | 64 | 8.1 MB |
| `scripts/verify/__pycache__` + 散落 `.pyc` | 23 | 1.1 MB |
| `NVIDIA Corporation`（空目录） | 4 | 0 MB |
| `_heal-driver2.cjs`（孤儿脚本，2026-09-20 补删） | 1 | <1 KB |
| **合计** | **482** | **≈880 MB** |
