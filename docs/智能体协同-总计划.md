# 智能体协同 · 总计划(对齐 GrokBot 无感)

## 愿景

做一个像 GrokBot 的 AI 工作台:用户只管委派,界面越少让用户操心越好。判据:「这帮用户委派了,还是又多给他一件事管?」

## Grok 的取舍(对齐目标)

- 只留 Bots/Chats/Prompts/Tools/Artifacts 五个概念,其余藏起来
- 主对象是 Bot 不是对话;头像即状态(idle/thinking/working/waiting/blocked/done),不做六个指示器
- 砍掉仪表盘/指派板/手动交接;协同往来进对话流、折叠成一行摘要
- 用户自发造 Chief-of-Staff 做路由;Routines 定时/事件触发
- 描述=长期规矩,对话=这一次的活

## 架构地图

- `apps/server` 后端(Fastify+PG):`toolLoop.ts`(脑)、`routes/*`、`orchestrator/*`(委派/名单)、`memoryShared/memoryNormalize/memoryHygiene`(记忆)
- `apps/desktop` Electron:`driver.ts`(手)、`src/browser/*`(浏览器组件)、`App.tsx`(界面)
- `packages/shared` 类型与默认值
- 浏览器是单实例组件:`embedWcId` 单值、所有 tab 同舞台 z-index 分层、登录态按项目分区。换前端时这些是功能,不能碰

## 已完成(新会话,均 push)

- 记忆四批:单一 `memories` 表+两级作用域(fe61ece)→会话三级(19be75e)→整理逻辑统一 memoryShared(19e57cc)→确认卡 UI(bf4a057)
- 补回:coordinatorPersona+identityBlock+建完能改人设(89e86c4)
- 记忆卫生:sleeptime 合并/标 merged/上限 30/20(d661854)

## 下一步(按顺序,每批:验收脚本+反证+立刻 push)

1. 无感核心:协同进对话流+删内部频道抽屉(数据/事件做,折叠卡长相归前端)
2. 状态承载到联系人(头像即状态)
3. 总协调路由(管家判断自己干/派谁)
4. 定时/事件触发(Routines)
5. 上下文压缩(toolLoop 只增不减,长任务必爆)——做学习内核的前置

## 铁律与坑

- 每批立刻 push;沙箱连 GitHub 常断,本地 commit 不保命
- 「源码不该有 X」的断言先剥注释;python replace 前先 assert;全文 includes 先确认唯一
- 分工:界面/文案/版式归用户(后续单独设计前端,1:1 还原),我们只做数据/接口/能跑通
- 前端等用户的 HTML/CSS,不阻塞主线

## 已知技术债

上下文只增不减、记忆检索仅字面匹配、无群聊、技能只有通道无内容、`index.ts` 硬编码 0.0.0.0、无重启恢复
