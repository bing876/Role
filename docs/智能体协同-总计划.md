# 智能体协同 · 总计划(对齐 GrokBot 无感)

> ★ 本文件是**跨会话的状态留痕**。会话意外死掉时,下一个会话读这一段就能接着干,不用重新摸。
> 每批做完必须更新「进度」段:已完成 / 未完成 / 已知缺口,写清到哪个 sha。

## 愿景

做一个像 GrokBot 的 AI 工作台:用户只管委派,界面越少让用户操心越好。判据:「这帮用户委派了,还是又多给他一件事管?」

## Grok 的取舍(对齐目标)

- 只留 Bots/Chats/Prompts/Tools/Artifacts 五个概念,其余藏起来
- 主对象是 Bot 不是对话;头像即状态(idle/thinking/working/waiting/blocked/done),不做六个指示器
- 砍掉仪表盘/指派板/手动交接;协同往来进对话流、折叠成一行摘要
- 用户自发造 Chief-of-Staff 做路由;Routines 定时/事件触发
- 描述=长期规矩,对话=这一次的活

## 架构地图

- `apps/server` 后端(Fastify+PG):`toolLoop.ts`(脑)、`routes/*`、`orchestrator/*`(委派/名单)、`memoryShared/memoryNormalize/memoryHygiene`(记忆)
- `apps/desktop` Electron:`driver.ts`(手)、`src/browser/*`(浏览器组件)、`App.tsx`(界面)
- `packages/shared` 类型与默认值
- 浏览器是单实例组件:`embedWcId` 单值、所有 tab 同舞台 z-index 分层、登录态按项目分区。换前端时这些是功能,不能碰

---

## ★ 进度(2026-09-24 更新)

### 基线

- 远端 `main` = `ac75d63`(Merge PR #9),含**批次 A–I + 修 1–5 + 收尾 1–5**,共 30 个提交。
- 本会话工作分支:`arena/01a0d0a8-role`(**不开 PR、不合并**,合并由用户在项目收尾时手动做)。

### 已完成(本会话,均已 push)

| 批次 | 内容 | 验收 |
| --- | --- | --- |
| **收尾 7**(2026-09-24) | 修 **H/I 两批的空转**。**H(电脑三级可见度)**:批次 H 当年交付时验收全绿、实际一行没生效 —— ① 组件只被 `export`,**App.tsx 里根本没渲染**;② 持久化打的是 `fetch('/api/agents/:id/visibility')`,而服务端注册的是 `/agents/:id/visibility`(恒 404);③ token 自己摸 `localStorage.getItem('token')`,桌面真实 key 是 `workbench.token`(恒等于没带 → 401);④ 从来不 GET 回已存档位,且 `status` 档提前 `return` 不渲染 children(挂上去就等于「用户一收起,正在跑的那张页当场被卸载」)。修法:组件整份重写,导出 `visibilityUrl` / `loadVisibility` / `saveVisibility`(apiBase 与 token **由调用方给**,组件不摸 localStorage;读失败一律回 `null`,**绝不回落 `'status'`** 把「读不到」变成「用户选了收起」),children 恒在同一个 `__host` 宿主里(三档只改宿主周围那圈 chrome),接管档由「全屏黑遮罩」改成**不挡视线的横幅**(遮罩正好盖住用户该监督的页);App.tsx 真的渲染它 —— 且是 `BrowserPanel` 的**兄弟**节点而不是把面板塞进 children(塞进去 = 切档时 webview 换父节点被 React 重建,驾驶坐标全废);喂给组件的是真数据(智能体状态行 + 主进程报上来的最后一条步摘要 + 当前页标题)。两条设计边界写进断言:**可见度只改「看得见多少」,绝不改「跑不跑」**(切档不调 `/agent/loop/*`、不碰 agentStop/agentDrop/browserThrottle)。**I(模型路由)**:按用户 2026-09-24 拍板做**真路由** —— `case 'chat'` 原来简单/复杂两个分支的 `model` **都写 `chatModel`**,只有 reason 文案不同(日志看着像路由了,上游收到的还是同一个模型);现在复杂闲聊配了 `DEEPSEEK_MODEL_CHAT_COMPLEX` 就真选它,没配则回落 CHAT 且 reason **如实写「回落快速模型(没有换模型)」**;`MODEL_ROUTING_ENABLED=0` 仍是一键退回默认模型。**顺手修好一份一直在假红的旧探针**:`panel-visibility-coupling-probe.py` 有 4 条断言还停在第 25 步之前的机制(180px / 56vh / `expanded`),D3 还把 `min-height:0` 这个 flex 常规写法当成「0 尺寸」 | H 快层 `verify:visibility` **40/0**(★**真调**桌面本体导出的那三个函数 + 假 fetch 截住验地址/方法/鉴权头/请求体/返回口径,不再只做 `readFileSync + includes` —— 旧验收对上述四个病灶**一处都咬不住**);H 真库端到端 `verify:visibility:e2e` **26/0**(自己起真后端于 8795 + 真 PG `verifydb`,**用桌面自己的函数去打真路由**,回真库查 `agents.computer_visibility` 那一列;三档轮一遍、非法值 400、未登录 401、**别的账号改/读一律 404** 且库里没被改;两个测试账号跑完删净 2/2);I `verify:routing` **31/0**(①-2 就是用户要的那条证明:两条路选出的 `model` 字段**真的不同**;⑦-4 钉死 `isSimple` 先看「长度<10」的既有优先级)。变异反证 **HV1–HV9 全红**(HV1 不渲染→⑤-2、HV2 加回 `/api`→①、HV3 摸 localStorage→②-3+④-1、HV4 回落 status→⑤-6、HV5 提前 return→④-2、HV6 宿主 display:none→⑥-1、HV7 切档停任务→⑤-5、HV8 面板塞进 children→⑤-4、HV9 默认 takeover→④-4)、**RV1–RV6 全红**(RV1 复杂档 model 改回 chatModel、RV2 回落却谎称路由了、RV3 kill-switch 失效、RV4/RV5 判简单/复杂恒 false、RV6 tag 把 chat 认成 tool),旧探针修完 **15/15** 且配套反证 6 处缺陷全被抓到;三份 md5 + `git status` 逐字节还原。`npm run verify` / `npm run typecheck` / `npm run verify:db`(真库)三个 EXIT 全 0。报告 `docs/acceptance/收尾7-H电脑可见度接线-I模型真路由-验收报告.md`;新增 `verify:visibility`、`verify:visibility:e2e`、`verify:routing`(旧的纯文本 `computer-visibility.mjs` / `model-routing.mjs` 已删) |
| **补:longdigits 脱敏规则**(2026-09-24,用户拍板「批次 J 之后再做」) | **≥21 位连续纯数字串整段脱敏**,标签 `longdigits`(补上既有缺口:`card` 只命中 13~20 位、`idcard` 正好 18 位,所以 21 位流水号、两卡号紧贴无分隔的 34 位串以前**整段明文**落进 `tasks.payload.steps` 这个明文列)。按拍板**两处一起改**:边界只有一个来源 `const LONG_DIGITS_MIN = 21`,掩码(`VALUE_PATTERNS`)与判定(`detectSensitive`)都由它拼正则,全文件不许再出现硬编码的 `{21,}`。掩码表里 longdigits 排在 `otp`/`cvv` **之前**(那两条值上限只有 10/4,排后面会给 `验证码<21位数字>` 留 11 位明文尾巴),判定里排在既有几条**之后**(判定只决定拒绝时说哪句人话,放最后 = 既有输入的判定结果一个字不变)。`\b` 边界:夹在字母/下划线里的数字串(base64、token 形状)不涂,紧贴中文的照涂。`verify:redact` 原先 ③-7/③-8 两条「钉住漏网现状」的断言按新规则改口径(防漂移的职责交给 ⑤-1 的 12~30 位**逐长度对照表**,比两条点断言更严) | `verify:redact` **70/0**(原 57 + 新增 ⑤ 段 13 条);变异 **L1–L7** 全红(L1 边界挪到 25 → 61/9、L2 摘掩码 → 61/9、L3 摘判定 → 65/5、L4 把 longdigits 挪到 otp 之后 → 68/2、L5 判定正则带 `g` → 66/4、L6 判定硬编码第二份边界 → 69/1、L7 摘 `sensitiveLabel` 的 case → 68/2),还原 md5 + `git status` 逐字节一致;`npm run verify` / `typecheck` / `verify:db`(真库 175 PASS 行)三个 EXIT 全 0。反证脚本 `scripts/verify/redact-longdigits-revert-proof.py`;报告 `docs/acceptance/补-longdigits-验收报告.md` |
| **重建 2｜批次 J**(2026-09-24) | 会话中途 `@某智能体` 拉人:确定性解析器**只有一份**(`packages/shared/src/mention.ts`,服务端按包名引、渲染层按源码相对路径引、Electron 主进程运行时不引 shared);拍板1(`@` 必须紧跟名字)、拍板2(只第一个命中当发言人,其余进 meta 列表);**R-A** 被点名者忙/等 → 不静默改派,回一句「它在做什么 + 要不要等/换人」(且**不让正忙的那个替自己开口**);**R-B** @ 的就是当前发言人 → 视作没写 @;**R-C** 交给模型的正文剥掉 `@名字`(库里存原文);只写了 `@名字` → 不调模型、反问。换人轮用被点名者的人设/项目记忆/技能 + 会话前文;**用户 2026-09-24 追加两项拍板并已落地**:**决策1 = per_round**(`@` 只换这一轮谁开口,`conversations.agent_id` 一个字节不动,下一轮不带 `@` 自动回到原来那位;端到端 T10.0/T10.12 发车前后各 SELECT 一次对账)、**决策2 = allow_with_owner**(点名轮**照样能进工具循环**,但循环主人仍是会话自己那位 —— `loopAgentId` 回读会话 `agent_id`,wcId/页面状态/循环名额都不换人,被点名者不接管别人的页;发车轮的 `meta.mention.speakerAgentId` 跟着改成跑循环那位,与库里那句开场白对得上账;此拍板**推翻**了第一版「换人轮一律不发车」,连带撤掉桌面那次本地 `@` 解析(成了死代码)与 `vite.config.ts` 的 `fs.allow`,解析器第 ⑧ 段反过来钉住不许长回来;残留:发车轮里被点名者不说话,要不要加一句文案等用户拍,见报告 §9.7);`messages.speaker_agent_id` 落库(5 处助手写入)、`/chat/history` 回 `speaker:{id,name}`、4 处 SSE `meta` 恒带 `mention`;桌面气泡在**换人那一句**挂名字牌。顺手修 6 个真问题(见验收报告 §7):`sseLocal` 把 `\n` 写成 `\\n` 导致「对话式建智能体」确认句在界面上凭空消失、那两处 `done.messageId` 塞 `Date.now()` 假 id、`blocked` 被当成忙(停过一次循环就五分钟点不动)、`mem-merge-batch2` 两条咬字面量的老断言升级(要求没变,另加变异 M6 证明没改松)、
  `packages/shared/dist` 的「半份跟踪」会让新克隆的服务端启动即崩(`dist/index.js` 已跟踪、`dist/mention.js` 被 `dist/` 规则挡住)
  → 已 `git add -f` 补齐并加第 ⑩ 段闸、验收脚本自己在 ESM 里写 `require` 被 try/catch 吞成「跳过」的假绿也已修 | 四层全绿:解析器 **43/0**、决定层 **41/0**、渲染层 **35/0**、真机端到端(真后端+真 PG+桩模型,桩截下每次上游请求以证明 R-C;T10 专验两项拍板)**81/0**;`verify`/`typecheck`/`verify:db`(真库 175 PASS 行)三个 EXIT 全 0;变异 **M1–M8** 全红(M1 使 J1 变 25/18、M3 使 J3 变 32/9、M5/M7 各使 J4 变 33/2、M2/M4 使端到端变 12/4 与 16/2、M8 使 T10 变 10/5 —— M7/M8 是「把决策2 改回旧口径」的倒退注入)且 md5 + `git status` 逐字节还原。报告:`docs/acceptance/批次J-@点名换人-验收报告.md`;新增 `verify:mention:{parse,decision,desktop,e2e}` 与 `scripts/verify/mention-revert-proof.py` |
| **收尾 6 补丁**(2026-09-24,用户从真实返回样本里看出的 bug) | 脱敏占位符的「N 字」报错:`密码Zx9!secret`(10 位)报 `·10字` 对,`银行卡6222021234567890`(16 位)与 `身份证110101199003071234`(18 位)都报 `·79字`。**根因不是正则贪婪**:`replace` 回调的实参形状随「有没有捕获组」而变,`card`/`idcard` 两条用的是非捕获组 → 实参只有 `(match, offset, string)`,旧代码取 `m[2]` 拿到的是**整个输入串**(79 = 上一轮 password 替换后的整串长度);带 2 个捕获组的 `password`/`otp`/`cvv` 取到的是值,所以对。修法:按实参形状还原组数,无组时用 `match` 本身。**不是泄漏**(敏感值本来就被替换了),是给人看的信息错了 | 新增 `scripts/verify/redact-placeholder.mts` → `npm run verify:redact`(**57 项**,挂进 `verify` 主链最前面),断言一律**整串精确相等**(`includes` 那种在字数错成 79 时照样全绿,正是上一轮放它过去的原因);用户要的三组断言逐条落地(①字数==被替换段长度 12 条 ②多值连排各自正确 ③反例证明不吞非敏感内容);两个 DB 脚本的显示字段断言同步升级(真库 115 项、pglite 56 项)。反证 **R1**(退回旧写法)红 24 项 + pglite 红 2 项、**R2**(card 正则改真贪婪)红 8 项,均 md5 逐字节还原。报告第 10 节 |
| **收尾 6 条件验收**(2026-09-24,用户「有条件通过」后的三条必做 + 拍板 + 两件小事) | **条件1**:查清服务端**没有**任务列表接口,桌面 `App.tsx:3284` 读的是 `/agent/task/current` 的 `goal`,`tasks.title` 从不返回给客户端 → 停用不会让标题空白,故 title 保持 NULL(用户给的「会空白才改存脱敏摘要」条件不触发);另给该接口加 `displayTitle`(goal 过 `scrubTaskText` 脱敏、截 80 字)。**条件2**:`loop/pause` 改成**先加密再动任何状态**,拿不到 cipher → 500 `goal_encrypt_failed`,不认领回执、不标 paused、不写 `task_pauses(旧行为是「台账照写、goal_enc 置空」,会留内存挂着库里没台账的半成品)。**条件3**:`migrateTaskGoalEncryption` 逐行三分支 —— 明文在+密文缺失/解不开才自动加密;密文与明文一致才清明文;**不一致则两份都留、一行不动**,warn 出行 id 交人判断(每次启动重报)。**拍板**:不加密 payload 整列,改为 `scrubTaskText` 把摘要脱敏做到**值形状**级(密码/验证码/卡号/身份证/CVV);老明文列暂不 DROP;启动全表扫的水位线方案记进待办。**小事**:`r4-acceptance-HEAD.json` 进 .gitignore 并移出版本控制;README 补威胁模型(goal 加密只防库文件被偷,目标仍明文发模型、明文回桌面) | 真库 **87 PASS/0 FAIL**、pglite **54/0**、M9 真服务端 **21/0**;`verify`/`verify:db`/`typecheck` 三个 EXIT 全 0;反证的反证 **M9b**(拆 pause 的闸)红 7 项、**M9c**(拆 task/start 的闸、退回明文落库)红 5 项,均 md5 逐字节还原。新增 `npm run verify:db:mutate-nocipher`。报告第 9 节:`docs/acceptance/收尾6-goal加密-验收报告.md` |
| **重建 1｜收尾 6** | 加密 `task_pauses.goal` 与 `tasks.payload.goal`:两表加 `goal_enc`;`task/start` 加密失败**直接 500**(fail-closed);`pause` 写密文、明文列恒 NULL;读路径全部**解密优先、回退旧列**;`tasks.title` 停用(它是 `goal.slice(0,80)` 的第二份明文副本);启动跑 `migrateTaskEncryptionWithRetry` 幂等回填;更正 db.ts / checkpoint.ts 里「R2 已加 goal_enc」的错误注释(R2 只是决议,从未落地);更正 README 数据与安全段 | `scripts/verify/task-encryption-db.mjs`(真库 61 项) + `task-encryption-pglite.mts`(37 项),挂进 `verify:db`;8 个变异反证全红。报告:`docs/acceptance/收尾6-goal加密-验收报告.md` |

### 收尾 6 期间**真库抓出的真 bug**(只读源码发现不了,已修)

第一版回填条件是 `WHERE goal_enc IS NULL AND 明文还在`。真库取证 SELECT 发现存在第三种形状:
**`goal_enc` 已有值 + 明文列也还有值**(灰度/回滚期新旧代码各写一半、人工改库都会留下)。
那种行永远满足不了 `goal_enc IS NULL` → 明文**永远清不掉**,而只造「纯老行」的验收脚本**照样全绿**。
已改为按「明文还在不在」扫描;两边都有值时**保留已有密文、只清明文**
(明文那份可能是 title 的 80 字**截断值**,拿它覆盖密文会丢内容);密文解不开才用明文重加密。
回归闸:两个脚本都加了「残行」专段,反证 M8(把条件退回 `goal_enc IS NULL`)真库红 4 项、pglite 红 7 项。

### 批次 M(前端结构重构,进行中)

**选型变更(2026-09-24,用户撤回前一版)**:**不接 Tailwind、不接 shadcn** —— 仓库里已有 `workbench-ui`
(React 18.3.1 + Vite 6.0.7 + TS 5.7.2,与 `apps/desktop` 版本号逐字相同;已组件化
Sidebar 125 / Rail 72 / InputBar 148 / ChatArea 90 / Modals 137;13 个编号 CSS + `:root` 设计令牌,
已迭代到 Round 157+ 且照 PNG 逐像素还原)。改为**把 workbench-ui 的结构与样式合并进 apps/desktop**。

**切片顺序**:M0 冒烟网 → M1' 设计令牌(`01-tokens`+`02-base`+`99-theme`) → M2' Sidebar → M3' Rail →
M4' InputBar → M5' ChatArea(顺带修 `.msg` 双定义) → M6' Modals → M7' 其余 CSS →
M8' 逻辑与状态迁移(2188 行,最重,单独一片) → M9' 收口(删 `styles.css`,确认无残留引用)。
**每片**:搬组件必须连同它对应的编号 CSS 一起搬;`browser/` 一行不动;验收 + 必须红的反证 + 立刻 push。

**M0 已完成(2026-09-24,`3b0b7ba`/`151ec8b`)**:`npm run verify:shell` 17 PASS / 0 FAIL —
jsdom 挂载整个 `<App/>`,走真实路径开页,钉住「三列都在 + `BrowserPanel`/`ComputerVisibility` 互为兄弟 +
`<webview>` 祖先链 golden + 切档/切视图/切智能体不换元素 + 关光页必须卸载(有意路径①) + 宿主不许
display:none/尺寸归零」。反证 `app-shell-smoke-revert.py` **R1–R4 全红**(套 wrapper / 挂到 view 上 /
加 key / CSS display:none)。★ 反证当场抓出**假网**:断言把 jsdom 元素交给 `assert.equal`,失败时
inspect 环状 DOM 图导致进程被 OOM 杀(退出码 137) → 已改成 `assert.ok(a === b, 人话)`。
**硬规则的准确表述**(写进 M0):**除三条有意路径**(allTabs 归零 / 深休眠 / key 变化)之外,
祖先链与节点身份必须逐字节不变 —— 写成「webview 永远不卸载」会假红。

**★ 用户叫停 + 顺序重排(2026-09-24,第二批指令)**:
① **第四列(浏览器区)的位置决定重开** —— 等用户看完完成品预览再定,**不跑 `--update-golden`**;
② **M1'–M7'(搬 CSS 与组件)全部推迟** —— 设计语言还没定稿,现在搬后面要反复改。
**新顺序(阶段 1 全部与设计无关,按序做)**:① 逻辑抽离(最优先) → ② 收尾 8 → ③ 收尾 9 →
④ 批次 K → ⑤ 批次 L(只做后端:意图解析 / 身份与名字生成 / 自然语言建 `agent_routines`,
那一行问题 UI 留到阶段 2)。**Electron 升级(33 → 新版)排到阶段 3**,现在不动。

**阶段 1① 逻辑抽离(进行中)**:
- **片 1 `features/knowledge` 已完成**(2026-09-24):`shared/api.ts`(把 `API_BASE`/`TOKEN_KEY`/
  `authFetchJson`/`dbHint` 从 App.tsx 模块级原样搬出)+ `features/knowledge/{useKnowledge.ts,index.ts}`;
  App.tsx 3714 → 3595 行,**JSX 段逐字节没动**(35615 字符完全相同)。手法:**只换来源、不改名字**
  (同名解构),所以 3714 行里的 JSX 一个字都不用改。
  验收:`verify:shell` 17/0(golden 逐字节)、**新增 `verify:logic` 8/0**(挂整个 `<App/>` 点真实按钮,
  验列表/校验/删除/登出清理/切号守卫)、`typecheck` EXIT 0、全量 `npm run verify` **EXIT 0**、
  旧探针 `panel-visibility-coupling-probe.py` **15/15**(行号搬动过,证明它按内容扫)。
  反证 `app-logic-smoke-revert.py` **K1–K4 全红**(丢切号守卫 / 放宽扩展名 / 去掉本地过滤 /
  DELETE 不送 body)。记录:`docs/批次M-阶段1-逻辑抽离-记录.md`。
- 规矩:每片 **JSX 与 CSS 一行不动** → `verify:shell` golden 逐字节 → 本片行为验收 →
  反证全红 → md5 还原 → 立刻 push。**7 个"最新值镜像"ref 一个都不许删**(本片把
  `sessionRef`/`curProjectRef` 注入 hook,而不是另建一份)。

**用户四条拍板(2026-09-24)**:① 浏览器工作区做**独立第四列**(其祖先链 golden 会在那一片有意变更,
用 `--update-golden` 显式改,节点身份与"不许卸载"规矩照旧);② 「状态层四件套」
(谁在等谁状态条 / 待确认黄条 / 任务卡 / 头像状态圈)**一起拍**,规格书见
`docs/批次M-设计规格书-运行时状态层四件套.md`;③ M1'–M7' **连着做**,做完一批汇报;
④ workbench-ui 的 `contacts.ts` 以我从已入库产物恢复的那份为准。

**顺手堵的窟窿**:根 `.gitignore` 的 `data/` 是全局通配,把 `workbench-ui/src/data/` 整个吞了,
`contacts.ts` 一直提交不上来 → 仓库里的 workbench-ui **编译不起来**(5 处 import 全断,还缺
`MODELS`/`MODEL_LABEL` 两个导出)。已把 `.gitignore` 改成 `/data/`,并从已入库产物
`dist-single/index.html` 里**逐字段恢复**(校验脚本 `workbench-ui-recover-contacts.py`);
现在 `workbench-ui` 的 `typecheck` 与 `build` 都是绿的。

### 未完成(按序 —— 批次 J、longdigits、收尾 7 均已交付并 push)

**★ 2026-09-24 用户重排后的顺序**:阶段 1 = ① **逻辑抽离**(进行中,片 1 已完成,下一片
`features/memory`) → ② 收尾 8 → ③ 收尾 9 → ④ 批次 K → ⑤ 批次 L(只做后端)。
**M1'–M7' 搬 CSS/组件、浏览器区第四列,全部推迟到设计定稿(阶段 2/3)**,详见上方批次 M 段。

| 序 | 批次 | 要点 | 依赖 |
| --- | --- | --- | --- |
| 0 | **收尾 8** | ✅ **已完成(2026-09-25)**:`handoffs/*.md` 与 `board.md` 落盘前走 `redactForStorage`(goal 密文解不出时不写文件),`handoff.ts` 三道闸 + `verify:handoff:revert` H1/H2/H3 + `handoff-redact.mts` 6/0 | 无 |
| 1 | **批次 K** | ✅ **已完成(2026-09-25)**:`orchestrator/followup.ts` 挂在 routines 定时扫的 tick 上,扫两类「掉线的活」并**主动**往归属智能体主会话写【协同·催办】——① `agent_delegations` 超期未回(`status=running` 且 `deadline_at` 已过;服务端重启丢内存定时器后的孤儿,熔断没触发的那种)② `task_pauses` 长期未恢复(`resumed_at IS NULL` 且挂起超 30 分钟),挂起提醒带**解密后的** `goal_enc` 目标。幂等:两张表加 `last_followed_at`,同一条 30 分钟内只提醒一次;提醒是**持续**的(过间隔仍掉线就再喊)。`verify:followup` 12 断言 + 反证 K1/K2/K3 全咬住。见 `docs/acceptance/批次K-Routines主动跟进-验收报告.md` | 已在收尾 6 之后 |
| 2 | **收尾 9** | ✅ **已完成(2026-09-25)**:真 SIGKILL 双进程测试(`loop-kill9-db.mjs` 驱动 + `loop-kill9-worker.mts`,真 PGlite 文件库,工具执行后/结果落库前杀进程,重启后 open_url 恰好执行 1 次)。★ 验收当场抓到并修了修 3 的一个真缺陷:`executed_tool_ids` 列原来被 `?? usedTools` 兜底写成**工具名**(id 对名字,跨重启去重永远比不中)→ 2 行修复(startLoop 初始化 + saveCheckpoint 去兜底)+ 列纯净度断言 + 反证 M1/M2/M3 全咬住。见 `docs/acceptance/收尾9-修3真kill-9-验收报告.md` | 无 |
| 3 | **批次 L**(只做后端) | ✅ **已完成(2026-09-25)**:自然语言建 `agent_routines`。**L1** `orchestrator/routineParser.ts` 纯解析(句式 → {agentName, taskTemplate, triggerType, triggerConfig} + 自动 name/description;问句/闲聊/建智能体的话/一次性/<5 分钟间隔一律 null 回落 LLM),`verify:routine-parser` 36 断言 + 反证 L1a/L1b/L1c 全咬住。**L2** `orchestrator/routineCreate.ts` + chat.ts 挂钩:名册**精确**解析(解析不到回名单、不偷偷挑最像的)/去重(同 agent+节奏+任务已有 active 不建第二个,回「已经有一个了」)/`createRoutine` 真建/**一句话回话**(「已设成:X 每天 09:00 任务,结果发这里。」—— 不是确认框,结果去向不撒谎),`verify:routine-create` 15 断言(真 PGlite 库) + 反证 C1~C4 全咬住。三问 UI/身份卡留阶段 2。**至此所有与设计无关的后端全部完成** | 无 |

### 已知缺口(收尾 6 查到但没修,一条不藏;详见验收报告第 7 节)

1. `tasks.payload.steps` / `payload.doc.*` 仍是明文(R2 起「脱敏但不加密」的既有设计,不在 goal 口径内)。**2026-09-24 进展**:按拍板没加密整列,改成 `scrubTaskText` 把摘要脱敏做到值形状级(密码/验证码/卡号/身份证/CVV 挡得住,pglite ⑤-B 六条反例),但「明文存储」这件事本身没变
2. `tasks.result_enc` 解出来的文档正文含 goal 明文(文档要给人看,落库是密文列)
3. `task/current`、`task/doc`、`loop/pauses` 会把 goal 明文回给客户端(必须如此,否则任务卡/文档/恢复提示全空白)
4. 旧明文列没 DROP:`tasks.title`、`task_pauses.goal`(恒 NULL)。DROP 会让回滚到旧代码直接报错 → **用户已拍板(2026-09-24):暂不 DROP,留回滚能力,等批次 K 落地跑稳后再议**;已记进 `docs/待办-R2残留-goal明文-20260922.md`
5. 启动回填是全表扫描(JSONB 表达式用不上索引),几十万行会有启动开销 → **用户已拍板(2026-09-24):本批不做**,水位线(`schema_meta` 记已回填到的最大 id/时间)方案已写进 `docs/待办-R2残留-goal明文-20260922.md`
6. `handoffs/*.md` / `board.md` 明文落盘 → **收尾 8**
7. 记忆提取的 transcript 含 goal 明文(发给模型那一次);落库那头是密文列
8. 桌面端内存里持有 goal 明文(核查过:没打日志、没落盘);客户端侧不在本批范围
9. **密文与明文不一致的行,明文会一直留到人工处理**(条件3 明确选择的取舍):补偿控制只有启动日志那条 warn(列行 id、不列内容),没有做「自动开人工介入卡片」之类的后续动作 —— 那属于批次 K 的活。验收里不假装它干净:pglite 专门造了一条明文含敏感词的不一致行断言「照留不误」,真库兜底扫按 id 排除并断言排除数正好是 2
10. `displayTitle` 目前只有 `/agent/task/current` 一个出口在用,桌面那行渲染仍读 `goal`(功能没问题);等前端定稿时一起接
11. ~~**≥21 位连续数字串整段漏网**~~ → **已修(2026-09-24,补 longdigits)**:`card` 命中 13~20 位、`idcard` 正好 18 位,
    21 位流水号、两卡号紧贴无分隔(34 位)这类输入以前**完全不脱敏**,明文原样落进 `payload.steps`;
    现在 ≥21 位连续纯数字串整段抹成 `[已脱敏·longdigits·N字]`,`detectSensitive` 同步返回 `longdigits`
    (边界只有一个来源 `LONG_DIGITS_MIN`,两处由它拼正则;`verify:redact` ⑤-1 用 12~30 位逐长度对照表钉住两边同口径)。
    **仍未修的两条小残留**(都不是漏网,是标签/尾巴,均已在验收里钉住):
    (a) 18 位纯数字身份证被标成 `card` 而非 `idcard`(card 那条排前面先命中;标签不含内容,改顺序会连带改
    `detectSensitive` 的类别,本批不动);
    (b) **分组写法**超过 20 位时(`6222 0212 3456 7890 1234 5678`)`card` 抹掉前 20 位、尾巴 4 位留着 ——
    longdigits 只管**连续**纯数字串,不跨分隔符;4 位数字本身不构成卡号/流水号,改 `card` 上限会牵动
    ③-4/③-5 与判定侧既有口径,所以留着并写进 ⑤-7 断言(谁改谁会先看到)
12. 其余历史缺口照旧:前端未定稿、记忆/路由是模糊字面匹配非语义、生产 CSP 未加。(原"重启恢复的工具去重只有逻辑模拟测试"已于 2026-09-25 收尾 9 补上真 kill -9 双进程测试,并修掉 `executed_tool_ids` 列被工具名污染的缺陷。)
    ~~模型路由只按任务类型选~~ → **已修(2026-09-24,收尾 7)**:`chat` 这一类现在按任务复杂度真选模型
    (`DEEPSEEK_MODEL_CHAT` vs `DEEPSEEK_MODEL_CHAT_COMPLEX`),没配复杂模型就回落并**如实说明回落**;
    但「简单/复杂」的判定仍是**关键词 + 长度**的启发式,不是语义判断(这一条照旧,谁改判定优先级 `verify:routing` ⑦-4 会先红)

---

## 历史批次(均已在 main / ac75d63 里)

- 记忆四批:单一 `memories` 表+两级作用域(fe61ece)→会话三级(19be75e)→整理逻辑统一 memoryShared(19e57cc)→确认卡 UI(bf4a057)
- 补回:coordinatorPersona+identityBlock+建完能改人设(89e86c4)
- 记忆卫生:sleeptime 合并/标 merged/上限 30/20(d661854)
- 无感核心:协同进对话流+删内部频道抽屉(3121e77)
- 状态承载到联系人:头像即状态(b99e10a)
- 总协调路由:Chief-of-Staff(9330c6e)
- 定时/事件触发:Routines(f49fef8)
- 上下文压缩:contextCompress.ts 保留首条 system+目标+最近 8 步
- 批次 A–I:交接结构化 / 白板 / 路由升级 / 重启恢复 / 前端引导 / Skills / 自查五项 / 电脑可见度 / 模型路由
- 修 1–5 + 收尾 1–5:checkpoint 加密 fail-closed / board 落库锁 / 验收收编 / 批次 G 正名 / README

## 验收口径(用丢失代码换来的,不要破)

- 涉及数据库一律走**真库**:设 `VERIFY_DATABASE_URL`,起真服务端跑真链路。
  收尾 1 靠这个抓出「`DO $$` 写成 `DO $`」「`saveCheckpoint` 拿不到 cipher 会回退写明文」;
  收尾 6 靠这个抓出「残行永远回填不到」(见上)。
- 每批必须有**反证**:把生产代码改坏,确认验收会红。只测脚本里抄的副本不算验收(批次 G 就这样假绿过)。
- 验收必须证明「**用户能感知到**」,不是「代码存在」(批次 H 组件没挂进界面、批次 I 两种任务同一个模型,都验收 PASS 但实际空转)。
- 新增验收脚本一律挂进 `npm run verify:*`,并跑一次全量记 EXIT 码。

## 铁律与坑

- **绝不开 PR、绝不合并 PR**(上次合并 PR #9 后平台把会话标记关闭,之后推送全被拦,批次 J 与收尾 6 全丢)
- 每批做完**立刻 push**,报出远端 sha;本地 commit 不保命
- **绝不跑 `git clean -fd` / `git reset --hard` / `git checkout -- <file>`**(上次批次 J 的未跟踪新文件 `mention.ts` 被 clean 删掉)。新文件写完立刻 `git add`
- 备份 bundle 一律放**工作区内**(`/home/user/backups/`),不要放 `/tmp`(不在快照范围)
- 变异测试改坏生产代码前先把原件 `cp` 到工作区内,还原后 `md5sum -c` 逐字节校验
- **★ 沙箱会在回合之间重置(本会话实测两次)**:清掉 `node_modules` / `/home/user/pgverify` / `backups` / `verify-logs`,
  并可能把本地 `.git` 退回旧状态(未跟踪的新文件会丢)。标准恢复动作(**不是** `git reset --hard`):
  1. 证据一律**抄进 repo 里的 docs** 并**当批立刻 push**(沙箱外的日志/JSON 不算证据);
  2. 恢复本地:`git fetch origin` → `git reset --mixed <远端 sha>`(保住工作区文件,只挪 HEAD 与索引);
  3. 未跟踪的新文件写完**立刻 `git add`**(铁律 3);
  4. 可再生产物(`docs/acceptance/**` 里每跑一次就变的 json/jsonl/log)进 `.gitignore`,
     要长期留的数字抄进报告 —— 否则「每跑一次验收就脏一次树」,迟早被误提交进不相关的批次
- **★ `packages/shared/dist/` 每回合都会被抹成「已删除」(批次 M 侦查时实测第三次)**:该目录被快照排除
  (`.gitignore` 里有 `dist/`),可它又是**强制入库**的(fresh clone 起来要能直接跑服务端)。
  表现:`git status` 里 8 个 `packages/shared/dist/*` 显示 M/D,**看起来像有人删了构建产物**。
  ⚠️ **千万别顺手 `git add -A` 提交** —— 那会把 dist 从版本控制里删掉,让下次 clone 启动不了服务端。
  恢复(二选一,优先第 1 个):
  1. `npm run build -w @ai-workbench/shared` 重新构建(需要 `node_modules`,先 `npm install`);
  2. 只还原被沙箱抹掉的那份**已提交内容**:`git restore --source=HEAD --worktree packages/shared/dist`
     —— 这是**唯一**允许的 checkout 系命令,且仅限这个目录、仅在 `git status` 确认该目录只有 M/D、
     没有任何未提交改动时用(它恢复的是 HEAD 里的内容,丢不掉别人的活)。用完 `git status` 必须为空
- 「源码不该有 X」的断言先剥注释;python replace 前先 assert 唯一;全文 includes 先确认唯一
- **DDL 是模板字符串**:SQL 注释里写反引号会把模板截断(收尾 6 踩过,`tsc` 报 `TS1005`)
- 分工:界面/文案/版式归用户(后续单独设计前端,1:1 还原),我们只做数据/接口/能跑通
- 前端等用户的 HTML/CSS,不阻塞主线,不自己猜着做像素级还原

## 沙箱环境备忘(本会话实测)

- 没有 `node_modules`:`ELECTRON_SKIP_BINARY_DOWNLOAD=1 npm install --no-audit --no-fund`
  (electron 二进制下载会被 TLS 挡,跳过后 554 包 6 秒装完)
- 没有 `psql` / `docker`,`apt-get` 出网被拦 → 真库用 `embedded-postgres@17.10.0-beta.17`
  (npm 上有真 PostgreSQL 17.10 二进制),装在**仓库外** `/home/user/pgverify/`,不污染 repo 的 package.json;
  `start-pg.mjs` 起在 `127.0.0.1:55432`,库名 `verifydb`
  ★ initdb 必须显式 `--encoding=UTF8`,默认 C locale 会给 SQL_ASCII,中文验收数据会出问题
- `VERIFY_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/verifydb`
