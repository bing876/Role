# QA-02 修复报告 —— 建智能体去掉二次确认,说一次就建好

**日期**:2026-09-25(北京时间)
**分支**:`arena/01a0d0a8-role`
**用户拍板**:"去掉建智能体的二次确认,改立刻建好(你自己的规格)。三问 UI 仍留阶段 2,但'不挡你、立刻建'现在就要。带反证:发「建一个销售助手」一次就建好、不再问确认。"

## 改了什么

- `routes/chat.ts`:批次 E 的"先确认再建"两步流整体撤掉 —— `detectBuildIntent` 命中就直接 `buildAgentImmediately` 真建,回一句「已建好「销售助手」:…。直接和TA聊就行,对话里说"建一个XXX"就能继续建同事,不挡你。」落 user+assistant 两条消息(真 id、speaker_agent_id、统一 sse()),劫持 SSE 回话。
- `orchestrator/agentBuilder.ts`:`pendingBuildIntents` 内存表 + get/set/clear + 确认消息检测函数**整段删除**(死代码不留;删除注释里连旧函数名都不写,因为静态验收按字符串钉"不许回来")。
- `scripts/verify/self-check-fixes.mjs`(修 4 验收):改成新规格 —— pending 机制必须**不存在**、chat.ts 必须有 `buildAgentImmediately` + 「已建好」话术、不许有「确认就建」。

## 验收(真机 e2e,`verify:build-agent`)

真后端(dist build 出来的)+ 桩模型(只替上游 LLM,登录/建号/路由/落库/名单全是真的)+ PGlite 文件库(沙箱无真 PG;本测试不需要重启,持久化问题归 QA-09):

- **T1-a** 发「建一个销售助手」→ SSE 流完整(meta+done);
- **T1-b** 回话就是「已建好「销售助手」…」**一次请求内**,不问你;
- **T1-c** 全程不出现「确认就建」;
- **T1-d** 随后 `/agents` 真的多了这一个(数量 +1、名字对)—— 回库里查,不看源码;
- **T2** 问句「建一个智能体是什么意思」→ 不建,回落 LLM;
- **T3** 无关键词「今天天气不错」→ 不建,回落 LLM。

**6 PASS / 0 FAIL。**

## 反证(`verify:build-agent:revert`)

| 变异 | 拆法 | 咬住位置 |
|---|---|---|
| B1 建的动作被掏空 | `buildAgentImmediately(...)` 调用换成假结果(回话照说"已建好") | 红在 T1-d「库里真的多了」(数量 1→1)—— 说建了没建,比问确认更坏 |
| B2 二次确认死灰复燃 | 回话换回旧的「要建一个「销售助手」…确认就建?」 | 红在 T1-b + T1-c「全程不出现确认就建」 |

2/2 咬住,每次注入重新 build 再打 e2e(e2e 打的是 dist),源码逐字节还原(md5),还原后复跑 exit=0。

## 已知边界

1. **同名重复建**:再说一次「建一个销售助手」会再建一个同名的(本批规格只管"立刻建",智能体去重不在范围;agents 表本就没有 name 唯一约束)。
2. 三问引导表(who/tone/duty)留**阶段 2** —— 用户明说"三问 UI 仍留阶段 2,但不挡你、立刻建现在就要"。
