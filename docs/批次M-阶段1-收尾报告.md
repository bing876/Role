# 批次 M · 阶段 1①「逻辑抽离」——**收尾报告**

> 用户 2026-09-24 拍板：**片 7b 做完 = 阶段 1① 结束**，收尾报告必须交这五件：
> ① 预算脚本最终输出；② 剩余每块留因完整清单；③ 已抽出 hook + 胶水清单 + 生产调用点数（R2）；
> ④ 缺陷清单最终版；⑤ 一句「阶段 2 卡用户设计定稿、不自行开工」。
>
> 结论先放这儿：**阶段 1① 已完成**，`App.tsx` 从 3714 → **2748 行**（同口径可对账），
> JSX 区**逐字节未动**、`styles.css` md5 未变、`browser/` 一行未动。
> 推链：`fce6853`（片 7a）→ **`5fc6d0b`（片 7b）** → **`1c43c66`（F1 独立小提交）**。

---

## ① 预算脚本最终输出（逐字粘贴，数字一律引用脚本）

命令：`python3 scripts/verify/app-tsx-line-budget.py`（已挂在 `npm run verify:gates` **链首**）

```
=== apps/desktop/src/App.tsx 行数预算（总行 = 2748）===
桶                 总行      有内容行
imports           19        19
module_types     283        71
other_top          4         4
auth_screen      236       194
agent_guide       70        67
app_logic       1294       789
app_jsx          842       793
tail               0         0
------------------------------
合计              2748      1937
✓ 对账通过：各桶总行之和 2748 == 文件总行 2748
```

**口径**：总行 = `splitlines()` 行数（等值 `wc -l`）；「有内容行」= 去掉空行与纯注释行。
**对账**：各桶之和 == 文件总行（脚本自己会验，不通过就非零退出）。

### 纵向轨迹（同口径，全部来自脚本）

| 节点 | 总行 | `app_logic` | `app_jsx` | 提交 |
|---|---:|---:|---:|---|
| 阶段 1① 开工 | 3714¹ | 2871¹ | 842 | — |
| 片 7 之前（基线） | 3286 | 1817 | 842 | `e09607e` |
| 片 7a 之后 | 3241 | 1772 | 842 | `fce6853` |
| **片 7b 之后（现在）** | **2748** | **1294** | **842** | `5fc6d0b` |

> ¹ 开工值是**旧口径**（`split('\\n')` + 未分桶），事后无法精确复原 —— 按用户 2026-09-24 的要求，
> 现在一律以脚本输出为准，收尾报告只引用脚本。

**没动过的东西（三条硬证据）**

| 对象 | 判据 | 结果 |
|---|---|---|
| `App.tsx` 的 JSX 区 | 从 `export default function App()` 的 `  return (` 切到文件末尾，两份文本逐字节比较 | `HEAD ≡ NOW`：35615 字符 / sha256 `adb33aedd37dacab` |
| `styles.css` | md5 | `b77759716631eed69877f84c2f22b52f`（与开工时**相同**） |
| `browser/`（BrowserPanel / useBrowserWorkspace / …） | `git log --stat` 逐片核对 | **一行未动**（webview 祖先链 golden 仍绿） |

---

## ② 剩余每块留因完整清单（`App.tsx` 逻辑区 1294 行 / 62 块）

判据（用户定的）：**任何一块留在 App 的逻辑，都要能说出一句「它必须在这儿」的理由**；
说不出理由的就是漏抽的。下面按四类逐块列全，**没有「其他」这一类**。

### A. 7 个 feature hook 的调用与同名解构（组合层的连线本身，共 ~250 行）

| 块 | 调用点 | 为什么在这儿 |
|---|---|---|
| `useAuth` | `App.tsx:679` | 组合层是所有 hook 的装配处；**同名解构**保证下面 JSX 一个字不改 |
| `useTasks` | `App.tsx:714` | 同上；`curTask`/`taskDetailOpen`/`docNote` 由它来 |
| `useChat`（注入 16 个端口） | `App.tsx:898` | 同上；**必排 `useMemory` 之前**（`setChatNote` 是别的 feature 唯一往聊天里说话的通道） |
| `useMemory` | `App.tsx:970` | 同上 |
| `useProjects` | `App.tsx:1130` | 同上；`onEnterProject` 是注入进来的跨 feature 协调点 |
| `useKnowledge` | `App.tsx:1386` | 同上 |
| `useBrowserGlue` | `App.tsx:1508` | 同上；**保持 hook、不翻 Provider**（用户第三批指令），调用点 = 1 |
| `browserRef` / `loadAgentStateRef` 两个镜像 | `App.tsx:854` / `App.tsx:855`（赋值分别在 `1509` 与 `990`） | 浏览工作区（1508）与 `loadAgentState`（976）的定义**排在 `useChat`（898）之后**，而 `sendChat` 要用它们 → 只能「渲染期赋值、调用期读取」（本仓库既有做法，与 `chatsRef` 同一条规矩） |

### B. 组合层 state：21 个 `useState` + 10 个 `useRef`（脚本清点）

| 组 | 名字 | 为什么必须在这儿 |
|---|---|---|
| 跨 feature 共读的「当前是谁 / 在哪个项目」 | `curAgentId`、`curAgentRef`、`agents`、`agentsRef`、`agentStates`、`agentStatesRef`、`curProjectId`、`curProjectRef`、`agentProjectRef` | 聊天 / 记忆 / 资料 / 浏览器 / 胶水**都读它**。放进任何一个 feature 都会逼其余 feature 反向依赖它 —— 只有组合层能同时被它们读 |
| 主进程侧镜像（权威在主进程） | `task`、`settings`、`settingsRef`、`computerVisibility`、`bridgeInfo`、`keepaliveBusy` | 它们是**主进程状态机与配置的镜像**，由 App 里那两条事件分发 effect 写入；feature 拿不到那条通道 |
| 「等用户回话」两组（**写它的在 App 的事件分发里**） | `awaitResume`、`awaitResumeAgent`、`awaitResumeWc`、`agentAwaitInfo`、`agentAwaitAgent`、`agentAwaitWcId` | 写它们的回调在 `useEffect@1708`（主进程 'agent' 事件）里 → 值必须留在这儿，`sendChat` 用注入的四件套读/清 |
| 输入框与登录态 | `input`、`sessionRef` | 输入框是**左中栏共用控件**；`sessionRef` 是给所有异步回包取 token 用的最新值镜像 |
| 纯展示态（JSX 直读） | `agentNote`、`agentBusy`、`personaEditOpen`、`personaEditAgentId`、`personaEditDraft` | 只被本文件 JSX 读写（编辑态、提示行），没有 feature 语义 |
| 动作账本 | `lastUserWasOpenRef` | 「这一轮用户原话本身就是开页确认」的账本，**左栏与状态行也要读**（不只 `sendChat`） |

### C. 23 个函数 / effect 块（脚本清点，合计 640 行）——逐块理由

| 行数 | 块 | 为什么必须留在组合层 |
|---:|---|---|
| 146 | `useEffect@1708` | **主进程 'agent' 事件分发**：ask / done / note 要按 `wcId → agentId` 分别落进**聊天桶**、点**红点**、刷**可见度**；跨 feature 的扇出只能在这儿 |
| 103 | `useEffect@1602` | **主进程转发的 UI 指令**（open / show / hide / focus / state，含 F3 那套白屏防护）：同时动浏览器层与聊天 |
| 44 | `addAgent` | 建号要同时动 **名单 + 聊天桶 + 项目记忆 + 浏览器桶** —— 典型跨 feature 协调 |
| 37 | `quickBuildAgent` | 同上，建完立刻发第一句（要聊天那侧的 `sendChat`） |
| 35 | `deleteAgent` | 删号要连带清聊天桶 / 项目记忆 / 浏览器桶 |
| 30 | `onLogout` | **跨 feature 清场**的唯一协调点（登出/换号）：本身只剩"顺序"，两半各归各位 |
| 27 | `loadAgents` | 拉名单后扇出到 memory / knowledge / tasks |
| 27 | `useEffect@1396` | `session` 变化的总清场 + 首帧顺序（先项目、再名单与资料） |
| 24 | `toggleKeepalive` | 会话状态（服务端）+ 主进程保活，两边都要动 |
| 21 | `enterProject` | **片 4 就定下**：跨 chat / memory / knowledge 的协调点 |
| 21 | `tidyCurrentAgent` | 统一整理记忆：同时读 `chats` 与 memory |
| 18 | `fetchAgentsFor` | 纯数据获取，但要把 `agentProjectRef` 补写成"归属账本"（多个 feature 共用） |
| 13 | `selectAgent` | 切号 = 换一份聊天 + 换项目记忆 + 拉历史与状态（跨 feature） |
| 13 | `useEffect@1517` | 按当前智能体拉可见度档位；读回来若是"接管"要**顺带把浏览器前置**（`browser.showFullscreen()` 只有组合层够得着） |
| 12 | `loadAgentState` | 拉某个智能体的会话状态（`curState`/保活/确认按钮都读） |
| 12 | `savePersona` | 存人设 → 刷新名单 + 关编辑态（跨 feature：名单 + UI 态） |
| 12 | `useEffect@1587` | preload 桥自检（第 1 步的回归哨兵） |
| 10 | `lastUserGoalBeforeConfirm` | 派生值：从 `chatsRef` 里找最近一条用户消息（**`chatsRef` 是镜像，只能在这儿读**） |
| 9 | `openPersonaEdit` | 打开编辑态 + 填草稿（纯 UI 态） |
| 9 | `onChangeComputerVisibility` | 改档位要落服务端 + 同步浏览器前置（跨 feature） |
| 8 | `onSettingsChange` | 配置回填（不本地猜结果，用返回/广播的值）。**这一步是为 UI 阶段准备的** |
| 7 | `pickCreator` | 名单已按项目过滤，再加一道 `projectId` 比对（防过期名单） |
| 2 | `useEffect@1583` | 设置变化时刷资源占用（跟随广播，纯组合层的事） |

**外加 2 个纯派生 1–2 行**：`sidebarAgents`（按项目过滤后的左栏名单）、
`visAgent`/`visPage`/`curState`/`curConfirmed`/`awaitHere`/`browser`（给 JSX 直接用的取数）。

> 另有一大块**不在 `app_logic` 桶里**：`module_types` 283 行（两行注释 + 类型）与
> `auth_screen` 236 + `agent_guide` 70（**带 JSX 的组件，按纪律留 App**，等阶段 2 搬组件时一起走）。
> 这些不含跨 feature 逻辑，所以不在"留因"范围里；阶段 2 的 M1'–M7' 会连它们一起处理。

---

## ③ 已抽出 hook + 胶水清单 + 生产调用点数（R2 门禁实测）

`npm run verify:gates` → **PASS R2**：7 个 hook 在生产代码里**都只有 1 处调用点**
（多处 = 状态会被撕成多份，门禁会直接红）。

| 抽出的东西 | 文件 | 行数 | 生产调用点（唯一） | 里面是什么 |
|---|---|---:|---|---|
| `useAuth` | `features/auth/useAuth.ts` | 122 | `App.tsx:679` | 静默登录 / 改密码 / 登出（会话半段） |
| `useTasks` | `features/tasks/useTasks.ts` | 140 | `App.tsx:714` | 任务快照 / 结果文档 / 红点 / `CurrentTask` |
| `useChat` | `features/chat/useChat.ts` | 977 | `App.tsx:898` | 聊天状态与历史（7a）+ `sendChat` / `onSend` / `startAgentTask`（7b） |
| `useMemory` | `features/memory/useMemory.ts` | 223 | `App.tsx:970` | 用户/项目/待确认三份记忆 + 确认与遗忘 |
| `useProjects` | `features/projects/useProjects.ts` | 158 | `App.tsx:1130` | 项目列表 / 切换 / 新建（+ F1 修复） |
| `useKnowledge` | `features/knowledge/useKnowledge.ts` | 191 | `App.tsx:1386` | 知识库资料上传 / 删除 / 入库段数 |
| `useBrowserGlue` | `app/browserGlue.ts` | 219 | `App.tsx:1508` | 跨 chat × browser 的胶水（embedRect / loopGone / 可见度 / 保活） |
| （契约钉子） | `app/browserGlue.contract.ts` | 56 | — | 把形状**钉死在类型层**：变形在调用点编译红 |
| （共享小工具） | `shared/api.ts` | — | 被各 feature 共用 | `API_BASE` / `TOKEN_KEY` / `authFetchJson` / `dbHint` |

配套还有：`features/*/index.ts`（feature 之间只经各自 index 互访）、
`scripts/verify/app-logic-smoke.{mts,run.tsx}`（行为验收网）、`app-logic-smoke-revert.py`（变异反证）、
`gate-static-rules.py`（R1 DOM 禁入断言库 / R2 单调用点 / R3 反证必须带 expect 且每片 ≥1 user-visible）。

### 这一阶段跑出来的数（全部是当次命令输出）

| 项目 | 结果 |
|---|---|
| `npm run verify:logic`（行为验收网） | **42 PASS / 0 FAIL**（片 7b 新增 7 条：4 条用户看得见 + 3 条守卫） |
| `python3 scripts/verify/app-logic-smoke-revert.py`（变异反证） | **34/34** 处缺陷被咬住（含 F1 的 P4） |
| `npm run verify:gates` | 4 条门禁全过（R1 / R2 / R3×2；R3 = 34 条注入全部带 expect、user-visible 片覆盖 A B C G K M P T） |
| `npm run verify:shell`（结构网：三列 / webview 祖先链 / 节点身份） | **17 PASS / 0 FAIL** |
| `npm run verify`（**全量链**） | **exit=0**（gates → redact → mention → tools → r4 → orch → websearch → memory → persona → collab → batches → db:pglite → shell → logic） |

**片 7b 的四条 user-visible 反证**（用户点名要看的那四件事，各一条独立变红）：

| 反证 | 注入 | 用户看见的差别 |
|---|---|---|
| B1 | 用户那句话不落桶 | 我发的话没出现在屏幕上 |
| B2 | 流式不再逐帧显示 | 推了第一帧，屏幕上却没有这半截 |
| B3 | 新一轮不清上一步骤 | 上一轮的步骤还挂在可见度条上 |
| B4 | 收到 `loopId` 却不记循环号 | 界面上没有「AI 任务执行中」那张任务卡 |

（B5 名字牌挂错人、B6 `chatsRef` 镜像失效、B7 桌面自己拦下「建一个」——三条守卫也各有一条。）

---

## ④ 缺陷清单最终版（`docs/批次M-已知UX缺陷清单.md` 为准）

| 编号 | 现象 | 状态 |
|---|---|---|
| **F1** | 新建项目那句「项目「X」建好了…」被随后的列表刷新擦掉（界面上等于没有确认） | ✅ **已修**（独立小提交 `1c43c66`）：刷新列表不再动提示文案；片 4 那条断言反过来；反证 P4 |
| **F2** | 「切换 / 新建项目」失败只写进 `projectNote`，成功时不写 —— **成功/失败共用一个位置**，用户不易分清 | ⏳ **待修，卡一个设计决定**（见下） |
| **F3** | `bridge.getTaskState().then(setTask)` 不校验返回值 → 三条路径可白屏（含用户点名的「重启后端」场景） | ✅ 已修（`4b4353b`，用户点名提前修）；验收 F3-A/A2/C1/C2；反证 F3-1/F3-2 |
| **F4** | `.authWrap` 一名两义（真登录页与"正在恢复登录状态…"占位页共用类名） | ⏳ 阶段 2 修（拆 `.authWrap--checking` / `--login`） |
| **F5** | **登出不清** `searchHint` / `runningLoopId` / `runningLoopWcId`（`resetChat` 有意逐字保留既有行为）：若在流式进行中登出，下次登录第一轮 meta 回来前会闪出上一轮的「🚀 AI 任务执行中」与输入框「发送补充」 | ⏳ 待修（独立小提交，3 行 + 1 条断言 + 1 条反证） |

**F2 需要用户拍一下呈现方式**（三条候选，都不动 CSS 布局）：
① 成功也写一行字（如「已经切到「X」」），靠**措辞**区分成功/失败；
② 失败文案统一加前缀（如「⚠ 切换没成：…」），成功保持现状；
③ 保持现状，留给阶段 2 连视觉一起改（拆成功/失败两个槽位 + 两种样式）。

**其它被有意保留的行为**（抽 hook 时逐字保存，没改）：7 个「最新值镜像」一个都没删
（`settingsRef` / `curAgentRef` / `sessionRef` / `lastUserWasOpenRef` / `curProjectRef` /
`agentsRef` / `agentProjectRef` / `agentStatesRef` + feature 内部的 `chatsRef` 等）；
webview 的三条有意卸载路径（`allTabs` 归零 / 深休眠 `.browserPanel__slept` / `key={t.id}` 重建）
之外，祖先链与节点身份逐字节不变（shell 网 17/0 + golden 未更新）。

---

---

## ⑥ 附：用户点名的几件事，现状一句话

| 用户问的 | 现状（都在代码里能指到） |
|---|---|
| **建智能体的意图检测在哪儿**（批次 L 的接点） | **桌面这一侧没有**任何本地判定 —— 桌面的接入点就是 `features/chat/useChat.ts:703` 那段注释「所有用户输入统一发送到服务端，服务端自主判定是否进入任务模式，不依赖本地正则白名单」，即 `message: value`（用户原话，逐字不改）那一行。真正的检测在**服务端**：`orchestrator/agentBuilder.ts`（`detectBuildIntent:74` / `isQuestionAboutBuilding:47`「问句不建」/ `isConfirmMessage:39` / `getPendingBuildIntent:29` / `setPendingBuildIntent:32` / `buildAgentImmediately:127`），由 `routes/chat.ts:491–550` 调用；确认话术是「要建一个「X」，职责：…，确认就建？」走**普通 `delta` + `done`**。★ 批次 L 要先解决一处重叠：桌面的 `CONTINUE_STRONG_RE`/`CONFIRM_ASK_RE` 判不出"这轮是建号确认"，而服务端的 `isConfirmMessage` 也认「继续/可以」。 |
| **`parseMention` 必须走 `packages/shared`（单一实现）** | ✅ 桌面 `apps/desktop/src/**` 全库 grep：没有第二份 `parseMention`；`mention-desktop.mts` ② 段有一条**死代码闸**（桌面任何源码里不许再 import/调用 `parseMention`、不许再有 `parseLocalMention`/`mentionRosterOf`）。片 7b 的守卫① 另有一条行为断言：`@卡布` 原话逐字送到服务端、`parseMention` 认人、`meta.mention` 回来后名字牌挂在 98 号（卡布）而不是当前智能体。 |
| **`chatsRef` 最新值镜像不许删** | ✅ 7 个镜像 ref 一个没删（`chatsRef` / `settingsRef` / `curAgentRef` / `sessionRef` / `lastUserWasOpenRef` / `curProjectRef` / `agentsRef` / `agentProjectRef` / `agentStatesRef` + 片 7b 新加的两个 `browserRef` / `loadAgentStateRef`）。行为断言：第 4 轮 `meta` 写回的会话号，第 5 轮请求体里必须读得到（B6 反证把它变成"读不到"）。 |
| **四件用户看得见的事，各一条独立变红** | ✅ B1 我的消息上屏 / B2 流式逐帧长出来 / B3 步骤逐条显示（新轮先清旧）/ B4 任务卡出现 —— 四条各自独立注入、各自命中（见 ③ 末尾表）。 |
| **三条守卫** | ✅ ① `@点名`换人照常（parseMention 走 shared + 名字牌挂对人）；② `chatsRef` 镜像；③ 建智能体（批次 L 接点，见上）。 |

## ⑤ 下一阶段：卡在用户的设计定稿上（**不自行开工**）

**阶段 2（M1'–M7'：搬 CSS / 搬组件 / 视觉 1:1 还原）在用户给出设计定稿前一行都不开；**
第四列（浏览器区位置）同样等用户看完成品预览再定，在那之前不推进、**不跑 `--update-golden`**；
阶段 1 剩下的顺序按用户 2026-09-24 的拍板走：**② 收尾 8（`redactForStorage`）→ ③ 收尾 9（真 `kill -9`）
→ ④ 批次 K → ⑤ 批次 L（只后端）**；Electron 升级排阶段 3。

（三不做：不开阶段 2 / 不动第四列 / 不开 PR、不合。）
